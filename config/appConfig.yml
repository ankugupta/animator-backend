logging:
  level:
    org.springframework.web : DEBUG
    org.springframework.data: ERROR

server:
  port: 9988
  servlet:
    context-path: /
    path: /animator



spring:
  data:
    mongodb:
      uri: "mongodb://localhost/animator"
    
